name: qr_borcode_scanner
description: A new Flutter Barcode&QR Code Scanner Project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

#IOS ICon Package
  cupertino_icons: ^1.0.2

#Barcode and QR Code Scan Package
  qr_code_scanner: ^0.6.1

#State Manegment Package
  flutter_bloc: ^8.0.1

#Responsive UI Package
  kartal: ^2.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
